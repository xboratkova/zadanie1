#include <stdio.h>
#include <time.h>
#include <stdlib.h>

main(){
	int pocet, min, max;
	printf("Zadajte kolko ma byt prvkov v poli:");
	scanf("%d", &pocet);
	printf("Zadajte rozpatie pri nahodnych hodnotach:");
	printf("Minimum:");
	scanf("%d", &min);
	printf("Maximum:");
	scanf("%d", &max);
	
	float pole[pocet];
	}
